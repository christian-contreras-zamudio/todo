// Desarrollado por www.iair.mx
   
function eliminar(url)
{
	if (confirm("Realmente desea eliminar este registro?"))
	{
		window.location=url;		
	}
}

function eliminarStatus(url)
{
	if (confirm("Realmente desea eliminar este registro?"))
	{
		window.location=url;		
	}
}


function eliminarPreFc(url)
{
	if (confirm("Realmente desea eliminar esta PreFactura?"))
	{
		window.location=url;		
	}
}

function eliminarAeropuerto(url)
{
	if (confirm("Realmente desea eliminar el Aeropuerto?"))
	{
		window.location=url;		
	}
}

function eliminarAerolinea(url)
{
	if (confirm("Realmente desea eliminar la Aerolinea?"))
	{
		window.location=url;		
	}
}

function eliminarProveedorExterior(url)
{
	if (confirm("Realmente desea eliminar el Proveedor?"))
	{
		window.location=url;		
	}
}

function eliminarClienteConsignneShipper(url)
{
	if (confirm("Realmente desea eliminar el Cliente?"))
	{
		window.location=url;		
	}
}

function eliminarUnidad(url)
{
	if (confirm("Realmente desea eliminar la unidad ?"))
	{
		window.location=url;		
	}
}

function eliminarCliente(url)
{
	if (confirm("Realmente desea eliminar este cliente ?"))
	{
		window.location=url;		
	}
}

function eliminarContactoAgente(url)
{
	if (confirm("Realmente desea eliminar este contacto ?"))
	{
		window.location=url;		
	}
}

function eliminarTracking(url)
{
	if (confirm("Realmente desea eliminar esta guia ?"))
	{
		window.location=url;		
	}
}
// global function
function eliminarRegistro(url, mensaje)
{
	if (confirm(mensaje))
	{
		window.location=url;		
	}
}

// global function
function confirmacion_accion(url, mensaje)
{
	if (confirm(mensaje))
	{
		window.location=url;		
	}
}