
{% if 'delete' in actions %}
    <a  onclick="return confirm('{{ "{{ 'crudMensaje.eliminar'|trans }}" }}?');" title="{{ "{{ 'crudMensaje.eliminar'|trans }}" }}" class="btn btn-danger btn-sm" href="{{ "{{ path('"~ route_name_prefix ~"_by_id_delete', { 'id': " ~ entity_singularized ~ "."~ identifier ~"}) }}" }}">
        <span class="fa fa-remove" aria-hidden="true"></span>
    </a>
{% endif %}
{% if 'show' in actions %}
    <a title='{{ "{{ 'crudMensaje.detalle'|trans }}" }}' class="btn btn-info btn-sm" href="{{ "{{ path('"~ route_name_prefix ~"_show', { 'id': " ~ entity_singularized ~ "."~ identifier ~" }) }}" }}">
        <span class="fa fa-eye" aria-hidden="true"></span>
    </a>
{% endif %}
{% if 'edit' in actions %}
    <a  title='{{ "{{ 'crudMensaje.editar'|trans }}" }}' class="btn btn-primary btn-sm" href="{{ "{{ path('"~ route_name_prefix ~"_edit', { 'id': " ~ entity_singularized ~ "."~ identifier ~" }) }}" }}">
        <span class="fa fa-edit" aria-hidden="true"></span>
    </a>
{% endif %}